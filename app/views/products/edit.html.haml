%h1 Editing product

= render 'form'

%br
%br

= link_to 'Show', category_product_path(category, product), class: 'btn btn-info'
\|
= link_to 'Back', category_path(category), class: 'btn btn-primary'
